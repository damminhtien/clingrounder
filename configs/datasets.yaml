synthetic:
  sample_notes: "data/samples/sample_notes.jsonl"
  gold: "data/samples/gold.jsonl"

nbme:
  enabled: false
  note: "Adapter placeholder. Provide local Kaggle NBME paths when available."

n2c2:
  enabled: false
  note: "Requires data use agreement."

mimic:
  enabled: false
  note: "Requires PhysioNet credentialed access."

codiesp:
  enabled: false
  note: "Spanish ICD coding adapter placeholder."

phase1_manual:
  gold_dir: "data/manual_gold"
  review_manifest: "data/manual_gold/review_manifest.jsonl"
  compiled_policy: "data/manual_gold/compiled/phase1_annotation_policy.yaml"
  conflict_report: "data/manual_gold/compiled/policy_conflicts.csv"
