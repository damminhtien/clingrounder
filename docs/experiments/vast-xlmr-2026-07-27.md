# Vast XLM-R Five-Type NER, 2026-07-27

## Decision

Promote:

```text
phase1-five-type-xlmr-qa-edu-e12-lr5e5-2026-07-27
```

The selected run reached exact-span F1 `0.723872` on the development chunks. The three-epoch
baseline collapsed to the `O` class, and the `3e-5` learning-rate control was worse than the
selected `5e-5` run.

This is a five-type token-classification experiment, not a generative LLM run. It therefore has no
prompt or decoding parameters. The Qwen branch was deliberately not run because its immutable
model revision, prompt text/hash, and decoding parameters have not been recovered.

## Immutable Inputs

| Input | Value |
| --- | --- |
| Git commit used for the selected training run | `87c8f3ed79bf42f1afcca43b8da28cdd0f7bf8e9` |
| Run spec | `configs/models/phase1-five-type-xlmr-qa-edu-e12-lr5e5-2026-07-27.yaml` |
| Run-spec SHA-256 | `83810f2d8036b2f4892bd2bff0c6bdbcc3d454d6cc86b47ffcd0f6b4c63e36ef` |
| Dataset SHA-256 | `093043d9294773d4faffc890c94f346d1c70c5d95181037b6ebf45c94de0d8f4` |
| Lock SHA-256 | `8ed9569bb6f221393de6508d2f36729f5b644b390336ab1eaf5bea7b12fcc87a` |
| Base model | `FacebookAI/xlm-roberta-base` |
| Base-model revision | `e73636d4f797dec63c3081bb6ed5c7b0bb3f2089` |
| Final-model fingerprint | `169a02de77d1198e062789271aac563c8000e8aa0ef7e4c95a1ed06ea07d771a` |

Remote processing received only the reviewed Round 1 train/development model dataset. Round 2,
frozen holdout labels, DUA data, and private/quarantined mining sources were not transferred.

## Runtime

The run used the maintained Vast.ai PyTorch template:

```text
image: vastai/pytorch:cuda-12.6.3-auto
template hash: 23077de9080ceec5b9202e517aa8c013
GPU: NVIDIA GeForce RTX 4090, 24 GiB
compute capability: 8.9
CUDA: 12.6
PyTorch: 2.12.0+cu126
Transformers: 5.13.0
precision: BF16
```

The template's Python 3.12/CUDA 12.6 environment was retained because resolving the entire lock on
that host selected an incompatible CUDA build. Top-level ML package versions were installed to
match the run contract where possible, while the template PyTorch build remained `2.12.0+cu126`.
Consequently, the lock hash identifies the intended environment but this run is not a byte-exact
`uv sync --frozen` realization. The complete observed package set is stored in
`environment-freeze.txt` beside each returned run. Reproduction must use both the image tag/template
hash and that freeze file.

One real forward/backward batch was executed before the full runs. It exposed and led to the
Transformers 5 `TrainingArguments` compatibility fix in commit `c499713`.

## Ablation Results

All metrics below are exact raw-span metrics on the same 20 development chunks with 431 gold
entities.

| Run | Epochs | LR | Precision | Recall | F1 | TP | FP | FN | Train seconds | Decision |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| pinned baseline | 3 | `2e-5` | 0 | 0 | 0 | 0 | 0 | 429 | 32.2 | reject |
| LR control | 10 | `3e-5` | 0.6502 | 0.7063 | 0.6771 | 303 | 163 | 126 | 75.0 | reject |
| recovery | 12 | `5e-5` | 0.6854 | 0.7669 | 0.7239 | 329 | 151 | 100 | 116.2 | keep |

The baseline's 429 gold spans differ from the dataset manifest's 431 entities because two source
spans cannot be represented exactly by the tokenizer under the configured alignment policy.

## Development Calibration

Calibration used 16 development documents and left the frozen holdout sealed. Selected confidence
thresholds:

```yaml
DISEASE: 0.8
DRUG: 0.8
LAB_RESULT: 0.5
LAB_TEST: 0.8
SYMPTOM: 0.8
```

The calibrated model-only output had:

| Metric | Value |
| --- | ---: |
| text score | `0.704111` |
| matched / gold / predicted entities | `384 / 431 / 438` |
| missing entities | `47` |
| spurious entities | `54` |
| boundary errors | `65` |

The benchmark's aggregate score, assertion score, and candidate score are diagnostic only here:
the model-only pipeline intentionally disables context and linking. Promotion must be decided by
entity text metrics and then by a separate rule/model/hybrid comparison.

## Returned Artifacts

The selected model is stored locally at:

```text
outputs/models/phase1-five-type-xlmr-qa-edu-e12-lr5e5-2026-07-27/
```

It includes `final-model/`, `run_manifest.json`, `train.log`, `environment-freeze.txt`, and local
versus remote SHA-256 inventories. All returned files matched the remote SHA-256 inventory.

Calibration artifacts are stored at:

```text
outputs/models/calibration-e12-lr5e5-2026-07-27/
```

Rejected-run manifests, logs, and environment freezes are retained under:

```text
outputs/models/vast-diagnostics/
```

Verify the returned artifact without importing the ML runtime:

```bash
uv run medical-kg model inspect-token-classifier-run \
  --config configs/models/phase1-five-type-xlmr-qa-edu-e12-lr5e5-2026-07-27.yaml
```

The Vast instance was stopped after artifact verification. The observed credit delta was about
`$0.30`; no account top-up or other billing action was performed. A stopped instance can still
incur storage charges and must not be destroyed until the owner explicitly authorizes that action.

## Next Gate

1. Reproduce model-only inference with the calibrated pipeline.
2. Generate rule-only and hybrid outputs over the same development IDs.
3. Compare exact entity text metrics with the frozen holdout still sealed.
4. Promote the hybrid only if it improves development WER and preserves raw offset invariants.
5. Open the holdout once after arbitration thresholds are fixed.

