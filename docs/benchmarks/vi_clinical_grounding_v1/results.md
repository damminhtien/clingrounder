# Benchmark results

No result is committed yet. Run the reproducibility command and review the generated
`summary.json` before adding a result table here. This prevents placeholder numbers from being
mistaken for measured performance.
